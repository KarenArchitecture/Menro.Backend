[build]
builder = "nixpacks"

[env]
DOTNET_VERSION = "8.0"

[build.nixpacks]
runtime = "dotnet"
packages = ["dotnet-sdk_8"]

buildCommand = "cd Menro.Web && dotnet restore && dotnet publish -c Release -o ../out"

[deploy]
startCommand = "dotnet out/Menro.Web.dll"
